#include "holberton.h"

/**
 *_strlen - returns length of a string
 *@s: the string to check length
 *Return: Always 0
 */

int _strlen(char *s)
{
	char *s;
	int len;


	len = strlen(*s);
	_putchar(*s);

	return (0);
}
